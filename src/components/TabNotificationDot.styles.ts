import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  dot: {
    position: 'absolute',
    top: -3,
    right: -5,
    width: 10,
    height: 10,
    borderRadius: 5,
    backgroundColor: '#ef4444',
    borderWidth: 2,
    borderColor: '#0f172a',
  },
});
